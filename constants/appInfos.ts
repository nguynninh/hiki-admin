import { lovelyHome } from "@/app/fonts";

export const localDataNames = {
  authData: "authData",
};

export const appInfos = {
  logo: {
    name: "Hiki",
    url: "http://localhost:3000",
    font: lovelyHome.className
  }
}
